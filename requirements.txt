# Pinned, cloud-friendly requirements. Remove the spaCy model wheel from here
# because model packages can be platform specific; the app will download the
# small English model at runtime if needed.

streamlit==1.51.0
spacy==3.8.7
nltk==3.9.2
# Use an xgboost release with broad wheel availability on Linux manylinux
# (change if your deployment log suggests a different version).
xgboost==1.7.6
scikit-learn==1.3.2
numpy==1.25.4
scipy==1.11.3

# Optional: If your app uses other packages, add them here (pandas, pyarrow, etc.)
